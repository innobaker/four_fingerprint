# Run with `dart run ffigen --config ffigen.yaml`.
name: FourFingerprintBindings
description: |
  Bindings for `src/four_fingerprint.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/four_fingerprint_bindings_generated.dart'
headers:
  entry-points:
    - 'src/four_fingerprint.h'
  include-directives:
    - 'src/four_fingerprint.h'
comments:
  style: any
  length: full
